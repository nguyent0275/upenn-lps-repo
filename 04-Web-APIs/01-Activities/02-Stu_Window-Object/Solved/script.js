// Logs window object / holds the entire web api
console.log(window);

// Logs reference to the document in the window object (DOM)
console.log(window.document);

// Logs root element of document. (html and shows you everything inside)
console.log(document.documentElement);

// Returns head element of current document (head)
console.log(document.head);
